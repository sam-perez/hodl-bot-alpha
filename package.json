{
    "scripts": {
        "migrate": "knex migrate:latest",
        "invoke-local": "serverless invoke local --function execute_dca_strategy"
    },
    "dependencies": {
        "airtable": "^0.6.0",
        "axios": "^0.19.0",
        "coinbase-pro": "^0.9.0",
        "knex": "^0.18.0",
        "moment": "^2.24.0",
        "pg": "^7.11.0",
        "ramda": "^0.26.1"
    },
    "devDependencies": {
        "dotenv": "^8.0.0"
    }
}
