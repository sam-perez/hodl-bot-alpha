{
    "scripts": {
        "migrate": "knex migrate:latest",
        "invoke-local": "serverless invoke local --function execute_dca_strategy",
        "invoke-local-dev": "serverless invoke local --function execute_dca_strategy --stage dev"
    },
    "dependencies": {
        "airtable": "^0.6.0",
        "axios": "^0.19.0",
        "coinbase-pro": "^0.9.0",
        "knex": "^0.18.0",
        "moment": "^2.29.2",
        "pg": "^7.11.0",
        "ramda": "^0.26.1",
        "tunnel-ssh": "^4.1.4"
    },
    "devDependencies": {
        "dotenv": "^8.0.0"
    }
}
